# 투자본부 모니터링 시스템 환경변수 템플릿
# 이 파일을 .env로 복사한 후 실제 값으로 수정하세요

# ===========================================
# 기본 애플리케이션 설정
# ===========================================
DEBUG=true
APP_NAME=투자본부 모니터링 시스템

# ===========================================
# 데이터베이스 설정
# ===========================================
DATABASE_URL=sqlite:///./app.db

# ===========================================
# JWT 보안 설정
# ===========================================
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===========================================
# DART 공시 API 설정 (필수)
# ===========================================
DART_API_KEY=your-dart-api-key-here
DART_CHECK_INTERVAL=1800

# ===========================================
# 이메일 알림 설정 (필수)
# ===========================================
EMAIL_SENDER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here
EMAIL_RECEIVER=recipient@company.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# ===========================================
# 주가 모니터링 설정
# ===========================================
STOCK_UPDATE_INTERVAL=10
MARKET_OPEN_TIME=09:00
MARKET_CLOSE_TIME=15:35

# ===========================================
# WebSocket 설정
# ===========================================
MAX_WEBSOCKET_CONNECTIONS=50

# ===========================================
# 파일 경로 설정 (Windows/WSL2 호환)
# ===========================================
# Windows 예시: DATA_DIR=C:\2dept\backend\app\data
# WSL2 예시: DATA_DIR=/mnt/c/2dept/backend/app/data
# 설정하지 않으면 자동으로 OS에 맞는 기본값 사용
DATA_DIR=/mnt/c/2dept/backend/app/data
LOGS_DIR=/mnt/c/2dept/logs